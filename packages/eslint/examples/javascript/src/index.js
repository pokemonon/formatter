import * as path from 'path';
import eslint from 'eslint';
import { log } from '@/utils/common';

const number = 1;
const string = 'asdf';
const obj = {
    name: '', age: '', city: '', sex: '',
};

const arr = [1, 2];

switch (1) {
    case 1:
        break;
    default:
        break;
}
