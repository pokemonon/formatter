import { defineComponent } from 'vue'
import { log } from './utils/common'

export default defineComponent({
    setup() {
        return () => (
            <div
            >
                <div
                >h</div>
            </div>
        );
    },
});
