{
  "name": "formatter",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.4.1",
    "lerna": "^3.21.0"
  },
  "scripts": {
    "postinstall": "./init.sh",
    "clean": "lerna clean -y && rm -rf node_modules"
  }
}
